# Data

Dataset의 로컬 저장 경계다. 생성된 데이터는 source directory에 두지 않으며 `raw/`, `processed/`, `cache/`는 Git에서 제외된다.

현재 dataset은 없고, 외부 dataset 위치나 생성 결과를 이 repository에 복사하지 않았다. 향후 dataset release에는 provenance와 schema를 함께 기록한다.
