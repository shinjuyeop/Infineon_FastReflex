# Tests

Current suite mirrors the supported responsibilities: `test_simulation.py`,
`test_dataset.py`, `test_hazard.py`, `test_terrain.py`, and `test_training.py`.
Historical milestone-only assertions remain recoverable from Git history; reusable
causality, physical-oracle, integrity, feature, probability, Terrain, and
Hazard/Terrain-independence contracts live in these canonical tests.
